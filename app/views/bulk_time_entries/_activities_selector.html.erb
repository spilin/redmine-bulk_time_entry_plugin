<% if activities.present? %>
  <%= f.select :activity_id, options_from_collection_for_select(activities, :id, :name, selected: f.object.activity_id),{required: true}, class: 'activity_select' %>
<% else %>
  <em><%= l(:label_no_data) %></em>
<% end %>
